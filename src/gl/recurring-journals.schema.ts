// Re-exports the Drizzle tables for recurring journal templates.
// Tables are defined in src/db/schema/gl.ts alongside other GL schema.
export {
  recurringJournalTemplates,
  recurringJournalTemplateLines,
} from '../db/schema/gl.js';
